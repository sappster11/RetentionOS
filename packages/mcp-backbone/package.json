{
  "name": "@retentionos/mcp-backbone",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Backbone MCP server — the retrieval + memory layer for chat-with-everything: search across every subsystem and persist conversations.",
  "bin": {
    "retentionos-mcp-backbone": "src/index.ts"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@retentionos/db": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.9.2"
  }
}
